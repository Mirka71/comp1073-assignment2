// Make a class for order
class order {

    //specify attributes
    contact;
    size;
    flavour;

    // make constructor
    constructor(contact, size, flavour) {
        this.contact = contact;
        this.size = size;
        this.flavour = flavour;
    }
}
